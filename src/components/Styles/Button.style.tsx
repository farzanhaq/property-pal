import styled from "styled-components";
import { Button } from "antd";

export const StyledNavButton = styled(Button)`
  margin-left: auto;
  margin-top: 15px;
`;

export const StyledResultBookButton = styled(Button)`
  margin-left: auto;
`;

export const StyledLoginFormButton = styled(Button)`
  width: 100%;
`;
